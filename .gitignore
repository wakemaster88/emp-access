# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
# Das Repo baut mit npm (package-lock.json ist geführt). Ein mitgelieferter
# pnpm-Lock würde den Vercel-Build auf pnpm umschalten.
/.pnpm-store
/pnpm-lock.yaml
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
/tmp

# einmalige Diagnose- und Analyseskripte (bleiben lokal)
/scripts/tmp-*.ts

# old project
/emp-access-alt

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!hub/.env.example

# hub (lokaler Agent)
/hub/node_modules
/hub/.cache

# raspberry pi (Geräte-Konfiguration entsteht erst auf dem Pi)
/raspberry-pi/config.json
/raspberry-pi/audio-config.json
__pycache__/
*.pyc

# vercel
.vercel

# cursor: Projektregeln sind geführt, lokale Hooks nicht
/.cursor/hooks
/.cursor/hooks.json

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma
webcams-history.bundle
