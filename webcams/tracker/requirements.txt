fastapi==0.118.0
uvicorn[standard]==0.32.1
ultralytics==8.3.150
supervision==0.25.1
opencv-python-headless==4.10.0.84
numpy==2.1.3
# ByteTrack braucht den LAP-Solver (Hungarian-Matching)
lap>=0.5.12
# ALPR (Kennzeichenerkennung an der Doorbird) — beide ONNX-basiert,
# laufen auf CPU; zusammen ~80 MB Speicher.
# Der `[onnx]`-Extra zieht onnxruntime mit; ohne wirft der Import nur eine
# Warnung und LicensePlateDetector ist nicht verfügbar.
open-image-models[onnx]>=0.3.0
fast-plate-ocr>=1.0.0
# Doorbird-Snapshots (Digest/Basic-Auth)
requests>=2.32.0
